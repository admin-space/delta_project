apiVersion: v1
kind: Service
metadata:
  name: {{ include "mini-airbnb.fullname" . }}
spec:
  type: {{ .Values.service.type }}
  selector:
    app: {{ include "mini-airbnb.name" . }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: 8080

